# Parser GLP039 (Client-Side) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tambahkan fungsi murni `parseGlp039(arrayBuffer)` ke `index.html` yang membaca file Excel GLP039 (Laporan Ketersediaan Dana Detail dari SAKTI) di browser dan menghasilkan data hierarki + rumus pagu/realisasi siap pakai — fondasi untuk layar Pagu & Realisasi (tugas urutan-3 dan seterusnya, TIDAK dikerjakan di plan ini).

**Architecture:** Tiga lapisan fungsi murni tanpa DOM, tanpa dependency eksternal: `_unzipSheet` (baca struktur ZIP standar + `DecompressionStream` bawaan browser, mengembalikan Promise) → `_parseSheetXml` (uraikan XML jadi baris mentah lewat pemindaian teks manual, sinkron) → `_turunkanHierarki` (logika bisnis: hierarki 8 level + rumus pagu/realisasi, sinkron). `parseGlp039` merangkai ketiganya.

**Tech Stack:** Vanilla JS ES5 di `index.html` (satu file, tanpa build step). Verifikasi lewat `node` biasa (bukan Playwright) — semua API yang dipakai (`DecompressionStream`/`TextDecoder`/`DataView`/`Uint8Array`/`Blob`/`Response`) tersedia di Node modern maupun browser Chrome.

## Global Constraints

- **Tanpa dependency eksternal baru** — TIDAK ADA JSZip atau pustaka XML apa pun. Unzip ditulis manual (baca central directory ZIP), XML diuraikan lewat pemindaian teks manual (bukan `DOMParser`).
- **Gaya kode**: `var`+`function`, ES5 murni, KECUALI `_unzipSheet` dan `parseGlp039` yang mengembalikan `Promise` (dipakai lewat `.then()`) karena `DecompressionStream` inheren asinkron — ini SATU-SATUNYA bagian `index.html` yang memakai Promise, dan hanya lewat `.then()`, TIDAK memakai kata kunci `async`/`await`.
- **Rumus adalah salinan PERSIS** dari `docs/HANDOFF-DESKTOP.md` bagian 4 — jangan menyimpang: `realisasiSakti = Y ?? Z` (ambil `Y` bila ada, kalau tidak `Z`), semua nilai uang dibulatkan (`Math.round`) sebelum dipakai/dibandingkan.
- **Skema baris hasil** (per `docs/superpowers/specs/2026-07-28-parser-glp039-design.md`): `{program, programUraian, kegiatan, kegiatanUraian, kro, kroUraian, ro, roUraian, komponen, komponenUraian, subkomponen, subkomponenUraian, akun, akunUraian, item, uraian, pagu, lock, realLalu, realIni, realSd}` — field `*Uraian` adalah PERLUASAN dari skema dasar di `docs/HANDOFF-DESKTOP.md`, field lain namanya TIDAK boleh diubah.
- **Cakupan tugas ini: HANYA fungsi parser + verifikasi.** TIDAK ADA perubahan tampilan/layar (layar Unggah GLP039 adalah tugas urutan-8 TERPISAH, tidak disentuh di sini). TIDAK ADA endpoint `.gs` baru (parser murni frontend).
- **Berhenti total (`throw`/`reject`) vs cuma peringatan** — lihat Bagian 4 spec, disalin di Task 2 di bawah. "Angka nol yang salah lebih berbahaya daripada error" — jangan melemahkan kondisi galat demi membuat sesuatu "terlihat berhasil".
- **Verifikasi**: ad-hoc lewat `node`, ditulis-dijalankan-dihapus dalam sesi implementasi (pola yang sama dipakai berulang di seluruh proyek ini untuk `mobile.html`) — TIDAK dikomit sebagai file test permanen, konsisten dengan konvensi proyek ("GAS sulit di-unit-test otomatis", `CLAUDE.md`). Bukti hasil verifikasi (termasuk angka yang cocok/tidak cocok) dicatat di laporan tugas, bukan di file test yang tertinggal di repo.
- Spec sumber: `docs/superpowers/specs/2026-07-28-parser-glp039-design.md` (5 bagian + tabel Keputusan, disetujui pengguna 28 Jul 2026, SUDAH divalidasi terhadap 410 baris data acuan nyata saat brainstorming — Bagian 3/`_turunkanHierarki` sudah terbukti benar, bukan cuma rencana di atas kertas).

---

## File Structure

Satu file yang diubah: **`index.html`** — satu blok baru disisipkan tepat sebelum
`</script>` (baris 6007, setelah IIFE utama `})();` di baris 6006), diberi penanda
komentar `/* ===== PARSER GLP039 (fungsi murni, dipakai layar Pagu & Realisasi) ===== */`
sesuai aturan struktur `docs/HANDOFF-DESKTOP.md` bagian 7 ("satu blok per layar/
fungsi dengan penanda").

Tidak ada file baru dibuat, tidak ada file `.gs` disentuh.

---

### Task 1: Implementasi tiga lapisan parser

**Files:**
- Modify: `index.html:6006-6007` (sisipkan blok baru di antara `})();` dan `</script>`)

**Interfaces:**
- Produces: `parseGlp039(arrayBuffer)` — fungsi publik utama, `Promise<{rows, ringkasan, peringatan}>`. Fungsi internal (`_unzipSheet`, `_bytesToAscii`, `_parseSheetXml`, `_parseSharedStrings`, `_decodeXmlEntities`, `_turunkanHierarki`) tidak dipanggil dari luar blok ini pada tugas ini — dikonsumsi Task 2 (verifikasi) dan tugas-tugas layar di masa depan (di luar cakupan plan ini).

- [ ] **Step 1: Sisipkan seluruh blok parser**

Cari blok ini di `index.html` (baris 6003-6007, akhir file):

```js
  document.addEventListener('paste', _onPasteUpload);
  _applyRoleUI();
  _checkAuth();   // bila token valid → loadAll() → _refreshScanAktif()
})();
</script>
```

Ganti dengan (IIFE utama TIDAK diubah — blok baru disisipkan setelah `})();`
sebelum `</script>`):

```js
  document.addEventListener('paste', _onPasteUpload);
  _applyRoleUI();
  _checkAuth();   // bila token valid → loadAll() → _refreshScanAktif()
})();

/* ===== PARSER GLP039 (fungsi murni, dipakai layar Pagu & Realisasi) =====
 * Membaca file Excel GLP039 (Laporan Ketersediaan Dana Detail, ekspor SAKTI)
 * langsung di browser -- lihat docs/HANDOFF-DESKTOP.md bagian 4 dan
 * docs/superpowers/specs/2026-07-28-parser-glp039-design.md untuk aturan
 * bisnis lengkap. Tanpa DOM, tanpa dependency eksternal.
 */

/* --- Lapisan 1: baca struktur ZIP standar, bongkar isi berkas terkait --- */
function _unzipSheet(arrayBuffer){
  var view = new DataView(arrayBuffer);
  var bytes = new Uint8Array(arrayBuffer);
  function u32(off){ return view.getUint32(off, true); }
  function u16(off){ return view.getUint16(off, true); }

  var EOCD_SIG = 0x06054b50;
  var minOff = Math.max(0, bytes.length - 22 - 65536);
  var eocdOff = -1;
  for (var i = bytes.length - 22; i >= minOff; i--){
    if (u32(i) === EOCD_SIG){ eocdOff = i; break; }
  }
  if (eocdOff < 0) throw new Error('Berkas bukan ZIP/XLSX yang valid -- penanda akhir arsip (EOCD) tidak ditemukan.');

  var cdOffset = u32(eocdOff + 16);
  var numEntries = u16(eocdOff + 10);

  var CD_SIG = 0x02014b50;
  var entries = {};
  var pos = cdOffset;
  for (var e = 0; e < numEntries; e++){
    if (u32(pos) !== CD_SIG) throw new Error('Struktur ZIP tidak dikenali di entri ke-' + e + ' (bukan XLSX standar).');
    var method = u16(pos + 10);
    var compSize = u32(pos + 20);
    var nameLen = u16(pos + 28);
    var extraLen = u16(pos + 30);
    var commentLen = u16(pos + 32);
    var localOffset = u32(pos + 42);
    var name = _bytesToAscii(bytes, pos + 46, nameLen);
    if (name === 'xl/worksheets/sheet1.xml' || name === 'xl/sharedStrings.xml'){
      entries[name] = { method: method, compSize: compSize, localOffset: localOffset };
    }
    pos += 46 + nameLen + extraLen + commentLen;
  }
  if (!entries['xl/worksheets/sheet1.xml']) throw new Error('Berkas tidak berisi xl/worksheets/sheet1.xml -- kemungkinan bukan file GLP039/XLSX yang benar.');

  function ekstrak(entry){
    var lp = entry.localOffset;
    if (u32(lp) !== 0x04034b50) throw new Error('Header lokal ZIP rusak di offset ' + lp + '.');
    var nameLen2 = u16(lp + 26), extraLen2 = u16(lp + 28);
    var dataStart = lp + 30 + nameLen2 + extraLen2;
    var raw = bytes.slice(dataStart, dataStart + entry.compSize);
    if (entry.method === 0) return Promise.resolve(raw);
    if (entry.method !== 8) return Promise.reject(new Error('Metode kompresi ZIP tidak didukung (' + entry.method + ') -- hanya stored/deflate.'));
    var ds = new DecompressionStream('deflate-raw');
    var stream = new Blob([raw]).stream().pipeThrough(ds);
    return new Response(stream).arrayBuffer().then(function(buf){ return new Uint8Array(buf); });
  }

  var dec = new TextDecoder('utf-8');
  return ekstrak(entries['xl/worksheets/sheet1.xml']).then(function(sheetBytes){
    var sheetXml = dec.decode(sheetBytes);
    if (!entries['xl/sharedStrings.xml']) return { sheetXml: sheetXml, sharedStringsXml: null };
    return ekstrak(entries['xl/sharedStrings.xml']).then(function(ssBytes){
      return { sheetXml: sheetXml, sharedStringsXml: dec.decode(ssBytes) };
    });
  });
}
function _bytesToAscii(bytes, start, len){
  var s = '';
  for (var i = 0; i < len; i++) s += String.fromCharCode(bytes[start + i]);
  return s;
}

/* --- Lapisan 2: uraikan XML jadi baris mentah {rowNum, cells} --- */
function _parseSheetXml(sheetXml, sharedStringsXml){
  var shared = sharedStringsXml ? _parseSharedStrings(sharedStringsXml) : null;
  var rows = [];
  var rowRe = /<row[^>]*r="(\d+)"[^>]*>([\s\S]*?)<\/row>/g;
  var m;
  while ((m = rowRe.exec(sheetXml))){
    var rowNum = +m[1], body = m[2], cells = {};
    var cellRe = /<c r="([A-Z]+)\d+"([^>]*)\/>|<c r="([A-Z]+)\d+"([^>]*)>([\s\S]*?)<\/c>/g;
    var cm;
    while ((cm = cellRe.exec(body))){
      var selfClosing = !!cm[1];
      if (selfClosing) continue;
      var col = cm[3], attrs = cm[4], inner = cm[5];
      var typeM = /\bt="([a-zA-Z]+)"/.exec(attrs);
      var type = typeM ? typeM[1] : null;
      var value;
      if (type === 's'){
        var idxM = /<v>(\d+)<\/v>/.exec(inner);
        var idx = idxM ? +idxM[1] : -1;
        if (idx < 0 || !shared || idx >= shared.length)
          throw new Error('Rujukan teks bersama (sharedStrings) tidak ditemukan untuk sel ' + col + rowNum + '.');
        value = shared[idx];
      } else if (type === 'inlineStr'){
        var tM = /<t[^>]*>([\s\S]*?)<\/t>/.exec(inner);
        value = tM ? _decodeXmlEntities(tM[1]) : '';
      } else {
        var vM = /<v>([^<]*)<\/v>/.exec(inner);
        value = vM && vM[1] !== '' ? parseFloat(vM[1]) : null;
      }
      cells[col] = value;
    }
    rows.push({ rowNum: rowNum, cells: cells });
  }
  return rows;
}
function _parseSharedStrings(xml){
  var out = [];
  var siRe = /<si>([\s\S]*?)<\/si>/g, m;
  while ((m = siRe.exec(xml))){
    var text = '', tRe = /<t[^>]*>([\s\S]*?)<\/t>/g, tm;
    while ((tm = tRe.exec(m[1]))) text += tm[1];
    out.push(_decodeXmlEntities(text));
  }
  return out;
}
function _decodeXmlEntities(s){
  return s.replace(/&lt;/g,'<').replace(/&gt;/g,'>').replace(/&quot;/g,'"')
    .replace(/&apos;/g,"'").replace(/&amp;/g,'&');
}

/* --- Lapisan 3: turunkan hierarki 8 level + rumus pagu/realisasi --- */
function _turunkanHierarki(rawRows){
  var ctx = { program:null, programUraian:null, kegiatan:null, kegiatanUraian:null,
    kro:null, kroUraian:null, ro:null, roUraian:null, komponen:null, komponenUraian:null,
    subkomponen:null, subkomponenUraian:null, akun:null, akunUraian:null };
  var out = [], peringatan = [];
  var totalBaris = 'JUMLAH SELURUHNYA';
  var ringkasanRow = null;
  var akunSet = {}, programSet = {};

  for (var i = 0; i < rawRows.length; i++){
    var c = rawRows[i].cells;
    if (c.A === totalBaris){ ringkasanRow = c; continue; }
    if (c.Q === undefined || c.Q === null) continue;

    if (c.B != null){
      if (String(c.B).indexOf('.') < 0){ ctx.program = c.B; ctx.programUraian = c.D || ''; }
      else { ctx.kegiatan = c.B; ctx.kegiatanUraian = c.I || ''; }
    }
    if (c.C != null){
      if (String(c.C).indexOf('.') < 0){ ctx.kro = c.C; ctx.kroUraian = c.G || ''; }
      else { ctx.ro = c.C; ctx.roUraian = c.K || ''; }
    }
    if (c.E != null){ ctx.komponen = c.E; ctx.komponenUraian = c.J || ''; }
    if (c.F != null){ ctx.subkomponen = c.F; ctx.subkomponenUraian = c.L || ''; }
    if (c.H != null){ ctx.akun = c.H; ctx.akunUraian = c.M || ''; }

    if (c.N != null){
      if (!ctx.akun){
        peringatan.push('Baris ' + rawRows[i].rowNum + ': item POK "' + c.N + '" tanpa kode akun yang dikenali -- dilewati.');
        continue;
      }
      var realSd = (c.Y !== undefined && c.Y !== null) ? c.Y : c.Z;
      out.push({
        program: ctx.program, programUraian: ctx.programUraian,
        kegiatan: ctx.kegiatan, kegiatanUraian: ctx.kegiatanUraian,
        kro: ctx.kro, kroUraian: ctx.kroUraian,
        ro: ctx.ro, roUraian: ctx.roUraian,
        komponen: ctx.komponen, komponenUraian: ctx.komponenUraian,
        subkomponen: ctx.subkomponen, subkomponenUraian: ctx.subkomponenUraian,
        akun: ctx.akun, akunUraian: ctx.akunUraian,
        item: c.N, uraian: c.N,
        pagu: Math.round(c.Q || 0), lock: Math.round(c.S || 0),
        realLalu: Math.round(c.W || 0), realIni: Math.round(c.X || 0),
        realSd: Math.round(realSd || 0)
      });
      akunSet[ctx.akun] = true;
      if (ctx.program) programSet[ctx.program] = true;
    }
  }

  if (!ringkasanRow) throw new Error('Baris "JUMLAH SELURUHNYA" tidak ditemukan -- struktur berkas tidak sesuai format GLP039 yang dikenali.');

  var totalPaguHitung = 0;
  for (var j = 0; j < out.length; j++) totalPaguHitung += out[j].pagu;
  var totalPaguBerkas = Math.round(ringkasanRow.Q || 0);
  if (totalPaguHitung !== totalPaguBerkas){
    peringatan.push('Total pagu hasil hitung ulang (Rp ' + totalPaguHitung.toLocaleString('id-ID') +
      ') tidak persis sama dengan angka "JUMLAH SELURUHNYA" di berkas (Rp ' + totalPaguBerkas.toLocaleString('id-ID') + ').');
  }

  var realSdTotal = (ringkasanRow.Y !== undefined && ringkasanRow.Y !== null) ? ringkasanRow.Y : ringkasanRow.Z;
  return {
    rows: out,
    ringkasan: {
      pagu: totalPaguBerkas,
      realisasiSd: Math.round(realSdTotal || 0),
      serapan: totalPaguBerkas > 0 ? (realSdTotal / totalPaguBerkas) : 0,
      sisa: Math.round((ringkasanRow.AD !== undefined ? ringkasanRow.AD : 0) || 0),
      jumlahBaris: out.length,
      jumlahProgram: Object.keys(programSet).length,
      jumlahAkun: Object.keys(akunSet).length
    },
    peringatan: peringatan
  };
}

/* --- Fungsi gabungan, dipanggil dari layar Unggah GLP039 (tugas terpisah) --- */
function parseGlp039(arrayBuffer){
  return _unzipSheet(arrayBuffer).then(function(hasil){
    var rawRows = _parseSheetXml(hasil.sheetXml, hasil.sharedStringsXml);
    return _turunkanHierarki(rawRows);
  });
}
</script>
```

- [ ] **Step 2: Cek sintaks**

Run: `node -e "require('fs').readFileSync('index.html','utf8'); console.log('OK')"`
Expected: `OK`

Run: cek fungsi baru ada persis sekali masing-masing:
```bash
for fn in _unzipSheet _bytesToAscii _parseSheetXml _parseSharedStrings _decodeXmlEntities _turunkanHierarki parseGlp039; do
  n=$(grep -c "^function $fn(" index.html)
  echo "$fn: $n"
done
```
Expected: tiap baris menunjukkan `: 1` (tepat satu definisi masing-masing fungsi).

- [ ] **Step 3: Commit**

```bash
git add index.html
git commit -m "$(cat <<'EOF'
feat(desktop): parser GLP039 client-side (tiga lapisan fungsi murni)

Fondasi fitur Pagu & Realisasi (docs/HANDOFF-DESKTOP.md tugas
urutan-2): _unzipSheet (baca ZIP standar + DecompressionStream) ->
_parseSheetXml (uraikan XML manual, tanpa DOMParser) ->
_turunkanHierarki (hierarki 8 level + rumus pagu/realisasi).
parseGlp039 merangkai ketiganya. Belum dipakai layar apa pun --
tugas urutan-8 (layar Unggah GLP039) terpisah.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RSUfSs6GrLV1T58R1rHKJF
EOF
)"
```

---

### Task 2: Verifikasi mandiri (self-test) lewat `node`

**Files:**
- Create (sementara, di scratchpad, TIDAK dikomit ke repo): `/tmp/kt-glp039-verify.js`

**Interfaces:**
- Consumes: `index.html` hasil Task 1 apa adanya (dibaca dari disk, tidak disalin/diubah). Fungsi `_unzipSheet`/`_parseSheetXml`/`_turunkanHierarki`/`parseGlp039` diekstrak dari blok `<script>` dan dijalankan lewat modul `vm` Node (pola yang sama dipakai berulang di proyek ini untuk mengekstrak fungsi dari `mobile.html`/`index.html` tanpa browser).

- [ ] **Step 1: Tulis skrip verifikasi**

Buat file `/tmp/kt-glp039-verify.js`:

```js
const fs = require('fs');
const vm = require('vm');
const zlib = require('zlib');

// --- Ekstrak fungsi parser dari index.html, jalankan di sandbox Node ---
var html = fs.readFileSync('/home/user/kas-tunai/index.html', 'utf8');
var m = /<script>([\s\S]*)<\/script>\s*<\/body>/.exec(html);
if (!m) throw new Error('Blok <script> utama tidak ditemukan di index.html');
var scriptSrc = m[1];

var sandbox = {
  console: console,
  Uint8Array: Uint8Array, DataView: DataView, TextDecoder: TextDecoder,
  Blob: Blob, Response: Response, DecompressionStream: DecompressionStream,
  Promise: Promise, Object: Object, Math: Math, String: String, Error: Error,
  parseFloat: parseFloat, JSON: JSON,
  document: { addEventListener: function(){}, getElementById: function(){ return null; },
    querySelectorAll: function(){ return []; } },
  window: {}, navigator: { onLine: true }, localStorage: { getItem: function(){ return null; } }
};
sandbox.window = sandbox;
vm.createContext(sandbox);
try {
  vm.runInContext(scriptSrc, sandbox, { timeout: 10000 });
} catch (e) {
  // Bagian lain index.html (di luar parser) boleh gagal di sandbox minim ini
  // (mis. memanggil google.script.run yang tak ada) -- itu tidak menghalangi
  // definisi fungsi parser sendiri, yang dieksekusi sebelum baris pemicu itu.
  console.log('(Catatan: script utama melempar setelah definisi fungsi -- wajar di sandbox minim ini): ' + e.message);
}
var _turunkanHierarki = sandbox._turunkanHierarki;
var _parseSheetXml = sandbox._parseSheetXml;
var _unzipSheet = sandbox._unzipSheet;
var parseGlp039 = sandbox.parseGlp039;
if (typeof _turunkanHierarki !== 'function') throw new Error('_turunkanHierarki tidak berhasil diekstrak dari index.html');
if (typeof _parseSheetXml !== 'function') throw new Error('_parseSheetXml tidak berhasil diekstrak dari index.html');
if (typeof _unzipSheet !== 'function') throw new Error('_unzipSheet tidak berhasil diekstrak dari index.html');
if (typeof parseGlp039 !== 'function') throw new Error('parseGlp039 tidak berhasil diekstrak dari index.html');

var hasil = {};

// ===== 5a. Lapisan bisnis terhadap 410 baris data acuan nyata =====
var F_ACUAN = '/tmp/claude-0/-home-user-kas-tunai/718d324c-66ed-582a-92de-6e6c4c133f0b/scratchpad/handoff-desktop-zip/design_handoff_kas_tunai_mobile/contoh-data/glp039-baris-terbaca.txt';
var lines = fs.readFileSync(F_ACUAN, 'utf8').split('\n').filter(function(l){ return l.trim(); });
var rawRowsAcuan = lines.map(function(line){
  var mm = /^R(\d+):\s*(.*)$/.exec(line.trim());
  var rowNum = +mm[1], rest = mm[2];
  var parts = rest.split('|').map(function(s){ return s.trim(); });
  var cells = {};
  parts.forEach(function(p){
    var eq = p.indexOf('=');
    if (eq < 0) return;
    var col = p.slice(0, eq).trim(), val = p.slice(eq+1).trim();
    if (val === '') { cells[col] = ''; return; }
    var num = parseFloat(val);
    var kolomTeks = /^[A-P]$/.test(col);
    cells[col] = (!kolomTeks && !isNaN(num)) ? num : val;
  });
  return { rowNum: rowNum, cells: cells };
});
// Saring baris "N=" yang bukan pola "NNNNNN. uraian" -- artifak baris terpotong
// pada berkas rendering CONTOH ini saja (lihat spec, tabel Keputusan baris
// "N=Jurnal"), BUKAN aturan yang masuk ke parser produksi.
rawRowsAcuan = rawRowsAcuan.filter(function(r){
  if (r.cells.N == null) return true;
  return /^\d{6}\./.test(String(r.cells.N));
});

var hasilBisnis = _turunkanHierarki(rawRowsAcuan);
var cekAngka = [
  ['pagu', hasilBisnis.ringkasan.pagu, 20373920000],
  ['realisasiSd', hasilBisnis.ringkasan.realisasiSd, 12223943202],
  ['sisa', hasilBisnis.ringkasan.sisa, 8149976798],
  ['jumlahProgram', hasilBisnis.ringkasan.jumlahProgram, 2]
];
hasil.lapisanBisnisAngkaUtama = cekAngka.every(function(c){ return c[1] === c[2]; });
if (!hasil.lapisanBisnisAngkaUtama){
  cekAngka.forEach(function(c){ if (c[1] !== c[2]) console.log('  selisih: ' + c[0] + ' = ' + c[1] + ' (acuan ' + c[2] + ')'); });
}
hasil.lapisanBisnisSerapan = Math.abs(hasilBisnis.ringkasan.serapan - 0.5999799352309226) < 0.0001;
console.log('  (info, bukan syarat lolos/gagal) jumlahAkun aktual =', hasilBisnis.ringkasan.jumlahAkun, '-- dokumen menyebut 38, belum terjelaskan, lihat spec.');

// ===== 5b. Lapisan unzip + XML dengan berkas buatan sendiri =====
function buatEntriZipStored(nama, isiBuf){
  var nameBuf = Buffer.from(nama, 'ascii');
  var local = Buffer.concat([
    _u32(0x04034b50), _u16(20), _u16(0), _u16(0), _u16(0), _u16(0), _u32(0),
    _u32(isiBuf.length), _u32(isiBuf.length), _u16(nameBuf.length), _u16(0), nameBuf, isiBuf
  ]);
  var central = Buffer.concat([
    _u32(0x02014b50), _u16(20), _u16(20), _u16(0), _u16(0), _u16(0), _u16(0), _u32(0),
    _u32(isiBuf.length), _u32(isiBuf.length), _u16(nameBuf.length), _u16(0), _u16(0),
    _u16(0), _u16(0), _u32(0), _u32(0), nameBuf
  ]);
  return { local: local, central: central };
}
function buatEntriZipDeflate(nama, isiBuf){
  var comp = zlib.deflateRawSync(isiBuf);
  var nameBuf = Buffer.from(nama, 'ascii');
  var local = Buffer.concat([
    _u32(0x04034b50), _u16(20), _u16(0), _u16(8), _u16(0), _u16(0), _u32(0),
    _u32(comp.length), _u32(isiBuf.length), _u16(nameBuf.length), _u16(0), nameBuf, comp
  ]);
  var central = Buffer.concat([
    _u32(0x02014b50), _u16(20), _u16(20), _u16(0), _u16(8), _u16(0), _u16(0), _u32(0),
    _u32(comp.length), _u32(isiBuf.length), _u16(nameBuf.length), _u16(0), _u16(0),
    _u16(0), _u16(0), _u32(0), _u32(0), nameBuf
  ]);
  return { local: local, central: central };
}
function _u32(n){ var b = Buffer.alloc(4); b.writeUInt32LE(n >>> 0, 0); return b; }
function _u16(n){ var b = Buffer.alloc(2); b.writeUInt16LE(n & 0xffff, 0); return b; }
function bangunZip(entriList){
  var locals = [], centrals = [], offset = 0;
  entriList.forEach(function(e){
    locals.push(e.local);
    centrals.push(Buffer.concat([e.central.slice(0,42), _u32(offset), e.central.slice(46)]));
    offset += e.local.length;
  });
  var localBuf = Buffer.concat(locals);
  var centralBuf = Buffer.concat(centrals);
  var eocd = Buffer.concat([
    _u32(0x06054b50), _u16(0), _u16(0), _u16(entriList.length), _u16(entriList.length),
    _u32(centralBuf.length), _u32(localBuf.length), _u16(0)
  ]);
  return Buffer.concat([localBuf, centralBuf, eocd]);
}

var sheetXmlUji =
  '<row r="1"><c r="A1" t="inlineStr"><is><t>Teks langsung</t></is></c>' +
  '<c r="B1" t="s"><v>0</v></c><c r="C1"><v>2.037392E10</v></c></row>';
var sharedXmlUji = '<sst><si><t>Teks rujukan</t></si></sst>';
var zipUji = bangunZip([
  buatEntriZipStored('xl/worksheets/sheet1.xml', Buffer.from(sheetXmlUji, 'utf8')),
  buatEntriZipDeflate('xl/sharedStrings.xml', Buffer.from(sharedXmlUji, 'utf8'))
]);
var abUji = zipUji.buffer.slice(zipUji.byteOffset, zipUji.byteOffset + zipUji.length);

_unzipSheet(abUji).then(function(hasilUnzip){
  var raw = _parseSheetXml(hasilUnzip.sheetXml, hasilUnzip.sharedStringsXml);
  var cells = raw[0].cells;
  hasil.lapisanZipInlineStr = (cells.A === 'Teks langsung');
  hasil.lapisanZipSharedString = (cells.B === 'Teks rujukan');
  hasil.lapisanZipDeflateInterop = (hasilUnzip.sharedStringsXml === sharedXmlUji);
  hasil.lapisanZipNotasiIlmiah = (cells.C === 20373920000);

  // ===== 5c. Kondisi galat =====
  var galatUji = [];
  function harusThrow(nama, fn){
    try { fn(); galatUji.push({nama: nama, ok: false, pesan: '(tidak melempar apa pun)'}); }
    catch (e){ galatUji.push({nama: nama, ok: true, pesan: e.message}); }
  }
  harusThrow('EOCD tidak ditemukan', function(){
    // TextEncoder dipakai (bukan Buffer.from(str).buffer) -- Buffer Node
    // dialokasikan dari pool bersama, .buffer bisa membawa sisa memori pool
    // di luar string yang dimaksud dan membuat uji ini salah lolos/gagal.
    var abRusak = new TextEncoder().encode('bukan zip sama sekali dan lebih panjang dari dua puluh dua byte').buffer;
    return _unzipSheet(abRusak);
  });
  harusThrow('JUMLAH SELURUHNYA tidak ada', function(){
    _turunkanHierarki([{ rowNum: 1, cells: { A: 'baris lain', Q: 100 } }]);
  });
  harusThrow('sharedStrings dirujuk tapi tidak ada', function(){
    _parseSheetXml('<row r="1"><c r="A1" t="s"><v>0</v></c></row>', null);
  });
  hasil.galatSemuaTerpicu = galatUji.every(function(g){ return g.ok; });
  if (!hasil.galatSemuaTerpicu) galatUji.forEach(function(g){ if(!g.ok) console.log('  tidak melempar:', g.nama); });

  // Baris tanpa Q (catatan kaki) dilewati dengan benar.
  var hasilCatatanKaki = _turunkanHierarki([
    { rowNum: 1, cells: { A: 'JUMLAH SELURUHNYA', Q: 100 } },
    { rowNum: 2, cells: { B: '*Catatan kaki, tanpa Q' } },
    { rowNum: 3, cells: { B: 'DL', D: 'Program', H: '521211', M: 'Akun', N: '000001. Item', Q: 100, Y: 50 } }
  ]);
  hasil.catatanKakiDilewati = (hasilCatatanKaki.rows.length === 1);

  cetakHasil();
}).catch(function(e){
  console.error('GAGAL (tak tertangkap):', e);
  process.exit(1);
});

function cetakHasil(){
  var semuaLolos = true;
  Object.keys(hasil).forEach(function(k){
    var v = hasil[k];
    var lolos = (v === true);
    if (!lolos) semuaLolos = false;
    console.log((lolos ? 'PASS' : 'FAIL') + '  ' + k + (lolos ? '' : ('  -> ' + JSON.stringify(v))));
  });
  process.exit(semuaLolos ? 0 : 1);
}
```

- [ ] **Step 2: Jalankan skrip**

Run: `node /tmp/kt-glp039-verify.js`

Expected: 9 baris `PASS` (`lapisanBisnisAngkaUtama`, `lapisanBisnisSerapan`,
`lapisanZipInlineStr`, `lapisanZipSharedString`, `lapisanZipDeflateInterop`,
`lapisanZipNotasiIlmiah`, `galatSemuaTerpicu`, `catatanKakiDilewati`), exit code 0.
(Delapan kunci `hasil` -- hitung ulang saat menjalankan, jangan asumsikan jumlah
pasti tanpa melihat output sungguhan.) Satu baris info tambahan tercetak terpisah
("jumlahAkun aktual = ...") -- itu BUKAN baris `PASS`/`FAIL`, murni informasi.

- [ ] **Step 3: Bila ada `FAIL`, perbaiki lalu jalankan ulang**

Baca pesan `-> ...` untuk tahu skenario yang gagal. Ingat Bagian 5a spec SUDAH
divalidasi manual sebelumnya (angka acuan cocok persis) -- kalau `lapisanBisnisAngkaUtama`
gagal di sini, kemungkinan besar kesalahan ada di cara Task 1 menyalin kode
`_turunkanHierarki` dari spec (typo saat transkripsi), bukan logikanya sendiri.
Untuk skenario 5b/5c, ini pengujian BARU (belum pernah dijalankan saat brainstorming)
-- boleh jadi ada bug asli di `_unzipSheet`/`_parseSheetXml` yang perlu diperbaiki di
`index.html`. Perbaiki `index.html` (Task 1) bila itu sumber masalahnya, jalankan ulang
`node /tmp/kt-glp039-verify.js` sampai semua `PASS`. Bila ada perbaikan ke `index.html`,
commit terpisah dengan pesan yang menjelaskan skenario yang diperbaiki.

- [ ] **Step 4: Hapus skrip sementara**

Run: `rm /tmp/kt-glp039-verify.js`

(Tidak ada commit di task ini bila semua langsung `PASS` -- tidak ada perubahan
`index.html` yang perlu.)

---

## Self-Review

**1. Cakupan spesifikasi** (`docs/superpowers/specs/2026-07-28-parser-glp039-design.md`):
- Bagian 1 (`_unzipSheet`) → Task 1, disalin verbatim dari spec.
- Bagian 2 (`_parseSheetXml`/`_parseSharedStrings`/`_decodeXmlEntities`) → Task 1, disalin verbatim.
- Bagian 3 (`_turunkanHierarki`) → Task 1, disalin verbatim (kode yang SAMA yang sudah divalidasi terhadap data nyata saat brainstorming).
- Bagian 4 (`parseGlp039` + throw vs peringatan) → Task 1 (fungsi gabungan) + Task 2 skenario 5c (kondisi galat diuji satu-satu).
- Bagian 5 (rencana uji 5a/5b/5c) → Task 2, seluruhnya dijalankan (bukan cuma direncanakan) — 5a memakai angka acuan yang SAMA yang sudah terbukti cocok saat brainstorming, 5b/5c BARU dijalankan pertama kali di task ini.

Tidak ada bagian spec yang tanpa task.

**2. Pemindaian placeholder**: tidak ada "TBD"/"implement later"/"similar to Task N" —
setiap step berisi kode lengkap. Catatan: skrip Task 2 membangun ZIP uji dari nol
lewat fungsi `_u32`/`_u16`/`bangunZip` yang ditulis lengkap di skrip itu sendiri
(bukan diringkas "buat ZIP uji" tanpa detail).

**3. Konsistensi tipe/nama**: seluruh nama fungsi (`_unzipSheet`, `_parseSheetXml`,
`_parseSharedStrings`, `_decodeXmlEntities`, `_turunkanHierarki`, `parseGlp039`) dan
field skema baris hasil (`program`/`programUraian`/dst.) identik persis antara Task 1
(definisi) dan Task 2 (pemakaian lewat ekstraksi `vm`).

**4. Kode Task 1 SUDAH dijalankan sungguhan sebelum plan ini dianggap selesai** (bukan
cuma disalin dari spec dan dipercaya begitu saja) — seluruh Task 2 (5a/5b/5c) dijalankan
manual saat menulis plan ini, memakai kode Task 1 apa adanya. Ditemukan dan diperbaiki
**satu bug nyata yang tidak ada di draf awal spec**: regex pendeteksi tipe sel
`/\bt="([a-z]+)"/` cuma cocok huruf kecil, padahal nilai atribut standar XLSX untuk sel
teks-langsung adalah `inlineStr` (huruf S besar di tengah) — **karakteristik yang
disebutkan eksplisit di `docs/HANDOFF-DESKTOP.md` sebagai bentuk UTAMA berkas GLP039
sungguhan**. Tanpa perbaikan ini, setiap sel `inlineStr` (nama program/kegiatan/item —
semua teks) akan gagal terbaca tipenya dan salah dianggap sel angka, menghasilkan `null`/
`NaN` untuk semua uraian di file SAKTI asli — bukan cuma di uji buatan. Sudah diperbaiki
jadi `/\bt="([a-zA-Z]+)"/` di Task 1 Step 1 dan spec (Bagian 2), diverifikasi ulang lolos
lewat skrip Task 2. Satu perbaikan kecil tambahan (non-fungsional, kerapian uji semata):
skenario galat "EOCD tidak ditemukan" di Task 2 diubah dari `Buffer.from(str).buffer`
(bisa membawa sisa memori pool Node yang tidak terkait) ke `new TextEncoder().encode(str).buffer`
(buffer terisolasi) — supaya uji ini benar-benar menguji apa yang dimaksud.

---

## Execution Handoff

Rencana selesai dan tersimpan di `docs/superpowers/plans/2026-07-28-parser-glp039.md`. Dua opsi eksekusi:

1. **Subagent-Driven (rekomendasi)** — subagent baru per tugas, review dua tahap di antara tugas, iterasi cepat.
2. **Inline Execution** — eksekusi tugas demi tugas di sesi ini, batch dengan checkpoint untuk direview.

Mana yang dipilih?
